# ============================================================
# Amazon Seller Content Engine - 环境变量
# 复制此文件为 .env 并填写你的密钥
# ============================================================

# --- OpenAI (必需 — 用于 embedding + 内容生成) ---
OPENAI_API_KEY=sk-xxxxx

# --- 数据源 API (可选 — 未配置的数据源会自动跳过) ---
YOUTUBE_API_KEY=AIzaxxxxx
REDDIT_CLIENT_ID=xxxxx
REDDIT_CLIENT_SECRET=xxxxx

# --- Supabase 数据库 (推荐) ---
# 在 Supabase Dashboard → Settings → Database → Connection string 中找到
# 选择 "URI" 格式，Transaction mode (port 6543)
SUPABASE_DB_URL=postgresql://postgres.[your-project-ref]:[your-password]@aws-0-[region].pooler.supabase.com:6543/postgres

# Supabase API (可选 — 用于直接调用 Supabase REST/Auth)
SUPABASE_URL=https://[your-project-ref].supabase.co
SUPABASE_ANON_KEY=eyJxxxxx
SUPABASE_SERVICE_ROLE_KEY=eyJxxxxx

# --- 或者用普通 PostgreSQL ---
# DATABASE_URL=postgresql://user:pass@localhost:5432/content_engine
